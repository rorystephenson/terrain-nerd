node_modules/
dist/
pipeline/cache/
.DS_Store
*.log

# Cloudflare R2 credentials for uploading tiles. Never committed.
.env

# Generated by the pipeline: ~39 MB, and reproducible from the OSM extracts with
# `npm run extract:data && npm run build:data`. See "Data" in the README.
web/public/data/

# Reference screenshots of a third-party map, kept locally for colour matching.
# Not ours to redistribute — delete these lines if you want them in the repo.
tile-example.png
water-wrong.png
Screenshot*.png

# Local env overrides (emulator switches). Never committed.
*.local
